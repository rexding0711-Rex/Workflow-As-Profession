# pptx skill scripts

## 所需文件

`kimi_ppt_dsl.pyz` 是 Python zip 应用（~250MB），因超过 GitHub 100MB 限制未纳入仓库。

请从 Kimi Desktop 安装目录获取：
```
# WSL/Linux
cp /path/to/kimi-desktop/resources/kimi_ppt_dsl.pyz ./
```

或从 Kimi Desktop 官方渠道下载。
